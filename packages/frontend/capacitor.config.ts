import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.minihacks.phonedin',
  appName: 'Phoned In Translation',
  webDir: 'out',
  bundledWebRuntime: false
};

export default config;
